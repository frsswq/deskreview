---
import Layout from "../layouts/Layout.astro";
import WorkCardLayout from "../layouts/WorkCardLayout.astro";
import HeroText from "../components/HeroText.astro";

const heroTitle = "Our Work";
const heroDescription = "Market-ready branding strategies, <br/> competition-winning digital campaigns.";

const pageTitle = "Work";
const withFooter = true;
---

<Layout pageTitle={pageTitle} withFooter={withFooter}>
  <HeroText heroTitle={heroTitle} heroDescription={heroDescription} />
  <WorkCardLayout />
</Layout>
