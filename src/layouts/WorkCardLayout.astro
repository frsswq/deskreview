---
import Card from "../components/Card.astro";
import { workCardData } from "../data/workCardData";
---

<div class="flex w-full flex-col md:mb-8">
  <div
    class="grid w-full grid-cols-1 md:grid-cols-2 gap-x-4 gap-y-10 md:gap-y-8 px-4 pb-8"
  >
    {workCardData.map((card) => <Card {...card} />)}
  </div>
</div>
